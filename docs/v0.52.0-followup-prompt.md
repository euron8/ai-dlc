# Fresh-session prompt — v0.52.0 part 2

Paste everything below the line into a new session started in `/Users/n8/git/ai-dlc`.

---

You are continuing feature work on the ai-dlc distribution. Branch
`feat/v0.52.0-wire-the-enforcers` already exists (commit `b5787dc`, VERSION `0.52.0`,
unpushed). Read `CHANGELOG.md`'s `[0.52.0]` entry and `docs/context-hardening-notes.md`
R32 first — they carry the measurements behind everything below. Do not re-derive them.

v0.52.0 so far: wired the enforcers (`enforcement-map.yaml` `call_sites:` now required
and validated by `scripts/validate-enforcement-map.sh` I9/W1+W2), gave the distribution
its first CI (`.github/workflows/validate.yml`), made Rule 8's divergence predicate
scope-relative (`findings_critical_prior_scope`), prescribed the bounded-join beat in
the planning steps, added gate Check 25, and promoted `OVERRIDE-DRIFT-SECTION` to
`HARD-OVERRIDE-DRIFT-SECTION` so a stale override blocks `apply`.

Two workstreams remain. **They ship in this same release.** Do them in order — B
depends on A being settled, because A changes the very files B has to migrate.

## Workstream A — purge rationalization from agent-read core text

**The operator's rule, verbatim:** *"Putting those things in the core skill are how we
get agents 'reasoning' around the rules and gates."*

A rule that explains itself invites negotiation. A gate that justifies its own design
hands the agent an argument for why the design does not apply here. And a pointer from
a gate file to the rationale doc is a door: the agent walks through it and comes back
with a story instead of a verdict. v0.52.0 already stripped every instance the current
session *introduced* (9 files, −37 lines). What remains is **pre-existing, and the
operator has explicitly said it is NOT deliberate scar tissue.** Remove it.

### Scope: the 38 agent-read files only

`core/skills/ai-dlc/SKILL.md`, `core/skills/ai-dlc/steps/*.md`,
`core/team-roles/*.md`. **Out of scope:** shell scripts (not in the agent's context —
comments there are for maintainers and stay), `CHANGELOG.md`, `docs/**`, fixtures,
READMEs.

### Known sites (verify — the list is a starting point, not a closed set)

Rationale headers:

| file:line | what it is |
|---|---|
| `SKILL.md:1005` | *"Why this clause used to say 'warn-only — never blocks the pipeline,' and why that…"* |
| `steps/route.md:160` | *"**Why this blocks here and nowhere else.**"* |
| `steps/gate-validation.md:260` | *"**Why this is a gate-level check.**"* |
| `core/team-roles/adversary.md:155` | *"*Why this exists.* v0.46.0 told you a clean verdict was a valid outcome…"* |
| `core/team-roles/adversary.md:185` | *"*Why this exists.* On S289's `research-requirements` cycle the CRITICALs ran 3 → 4 → 7 → 0…"* |

Doors to the story (`docs/context-hardening-notes.md` pointers):
`SKILL.md` 380, 501, 520, 1294; `steps/_gate-procedures.md:72`;
`steps/gate-validation.md` 68, 117, 616; `steps/implementation.md:403`.

Also sweep for the same shape under other names: embedded sprint numbers (`S289`,
`S290`), measured anecdotes (`"the reference consumer's real breaches were 161%, 215%,
526%"` — `SKILL.md:994`), version archaeology (*"this used to say X"*), and
false-positive-cost prose that runs past a sentence.

### What MUST stay — do not over-cut

1. **Rule 26(c) contracts.** SKILL.md Rule 26(c) requires machinery to state *"the
   concrete failure it catches, the cost of a false positive, and the condition under
   which it is removed."* That is a **three-fact contract, not a rationale.** Keep it,
   in the terse `**Minimum mechanism (Rule 26(c)).** Failure caught: … False-positive
   cost: … Removal condition: …` form. See the Check 25 block in `gate-validation.md`
   for the target shape. A retro audit flags machinery that lacks one, so deleting
   these breaks a different gate.
2. **Factual API/mechanism statements.** *"`TaskOutput` joins a `task_id`, which only
   `TaskCreate` produces — `TaskOutput` cannot join an `Agent`"* is a fact an agent
   cannot argue with, and v0.50.0 put it inline precisely so it would not be
   re-introduced. Keep. The test: **can the agent use this sentence to argue the rule
   does not apply to it?** If yes it is rationale — cut. If it is an invariant of the
   world, keep.
3. **Anything a consumer override anchors to.** Overrides shadow core by *heading*.
   Deleting or renaming a heading silently orphans an override. Before removing a
   heading, grep `/Users/n8/git/graph/.claude/skills/ai-dlc/overrides/*.md` for
   `shadows:` entries pointing at it.

### Where the removed text goes

`docs/context-hardening-notes.md`, appended to the relevant R-section (or a new one).
Nothing is destroyed — it is relocated to where humans read it and the pipeline does
not. **Do not leave a forwarding pointer in the core file.** The pointer is the door.

### Verification

- `bash core/scripts/validate-reattach-budget.sh --skill core/skills/ai-dlc/SKILL.md`
  must PASS. It has ~253 tokens of slack; every byte cut above SKILL.md line ~389
  *buys* headroom, so this should get healthier, not worse. **Report the new slack.**
- `bash scripts/validate-enforcement-map.sh` must stay green (I6 ties headings to
  `CHECK_LOADED` anchors — a deleted heading will trip it).
- Full fixture suite green.
- Report the byte/token delta per file. This should be a meaningful net reduction in
  the resident and per-gate paths; if it is not, say so plainly.

## Workstream B — make `/ai-dlc-update` able to actually LAND v0.52.0 on graph

This is the point of the release. **v0.52.0's headline fix does not reach the reference
consumer without it.** Verified facts, do not re-litigate:

1. **Graph overrides the exact clause v0.52.0 rewrites.**
   `.claude/skills/ai-dlc/overrides/SKILL__Rule-8.md` (`base_sha: 8c706ca`) shadows
   `SKILL.md#Rule 8` and **copies the divergence clause verbatim**. The lead reads the
   override, not core. Un-migrated, the new scope-aware validator would enforce one
   predicate while the rulebook the lead obeys states the old one — a split-brain in
   which the fix is inert on the one pipeline it was written for. v0.52.0 already made
   this `HARD-`, so `apply` now **stops**. Stopping is not landing. The updater must be
   able to *carry the operator through the re-adoption*.

2. **Core now defines Check 25; graph's extensions already squat 25–33.**
   `.claude/skills/ai-dlc/extensions/checks/*.md` define bare `### 25.` … `### 33.` and
   have adopted **zero** v0.49.0 catalog labels (`grep -c "\[ext:"` → 0 in all four
   files). Extensions render additively into one numbered list, so `Check 25` becomes
   ambiguous and the bare number the lead writes into the gate log has no referent.
   `validate-layer-entries.sh` already reports this — as a **WARN, in no CI**, which is
   why it was ignored for three releases.

3. **Graph carries unregistered in-place core drift** — outside the layer system
   entirely, so no override/extension entry describes it and the reconcile cannot see
   it: `.claude/team-roles/tea.md` is a wholesale consumer rewrite with **no override
   entry**; `steps/retro.md` has an in-place edit that **duplicates** an existing
   override; `.claude/hooks/ai-dlc-continue.sh` is +80 lines vs upstream.
   `validate-layer-entries.sh` currently reports **5 errors, 21 warnings** on graph.

### What to build

Work in `core/skills/ai-dlc-update/`. Read its `SKILL.md` and
`reconcile/layer-drift.sh` before changing anything — the classifier already emits
most of the signal; the gap is that nothing *acts* on it.

- **A re-adoption workflow for `HARD-OVERRIDE-DRIFT-SECTION`.** On block, the updater
  must show the operator the core section's `base_sha..theirs` diff, the override's
  current text, and the override's stated `reason:` — then ask the one question that
  matters: *does upstream's change supersede the reason this override exists?* Outcomes:
  **retire** the override (upstream absorbed it), **re-adopt** (merge the new core text
  into the override, preserving the consumer's delta), or **re-affirm** (the override
  still stands). All three end in a re-stamped `base_sha`. **The operator must not be
  able to proceed by doing nothing** — that is the failure mode this release exists to
  end.
- **Mechanized catalog relabelling.** Extension checks colliding with core integers get
  `### <n>. [ext:<id>] <title>` per v0.49.0. The updater should offer to rewrite them,
  and `validate-layer-entries.sh` must go from WARN to **ERROR** for a collision with a
  core check number, and run in the consumer's CI. Confirm whether graph's
  `validate-layer-entries.sh` is core-owned and shipped by `install.sh` (it is in the
  hardcoded copy loop — check).
- **Unregistered-drift detection.** A core-manifest file modified in place with no
  override entry is invisible today. Detect it (compare installed core against the
  stamped distribution sha) and force it into the layer system — an override with a
  `base_sha`, or a revert. `tea.md` is the live case.
- **Judge correctness against the RENDERED pipeline, not core alone.** A change is only
  effective if it survives `overrides > extensions > core` resolution.

### Verification — this is the part that matters

Do **not** claim success from a green script. Prove it end-to-end:

1. Dry-run `/ai-dlc-update`'s reconcile against `/Users/n8/git/graph` (it is **paused
   mid-sprint at S290 discovery — do not advance its pipeline, do not write to its
   `_bmad-output/`**). Show that it now BLOCKS on `SKILL__Rule-8` and on the 25–33
   collision, and that each block has a path to resolution.
2. After migration, prove the Rule 8 fix is **live in the rendered pipeline**: graph's
   *effective* Rule 8 must contain `findings_critical_prior_scope`. Grep the override,
   not core.
3. Re-run `bash scripts/validate-layer-entries.sh` in graph: the 5 errors must be
   resolved or explicitly dispositioned.
4. Re-run `validate-adversarial-convergence.sh --series
   _bmad-output/planning-artifacts/s290-brief-adversarial-p` in graph. It must produce
   **the same result as before the update** (one `FAIL (D)` — the cycle genuinely has
   not converged; p8 stamped `EXIT_CONDITION_NOT_MET`). **Zero new failures on
   adoption, no back-fill of p1–p8** — retro-stamping a review artifact falsifies an
   audit record.

## Standing constraints

- **Never add rationale to `SKILL.md`, `steps/*.md`, or `core/team-roles/*.md`.** That
  is the whole of Workstream A; do not undo it in Workstream B.
- SKILL.md's first ~5000 tokens are whole-re-read on **every compaction**; the resident
  region sits ~253 tokens under a hard ceiling. Any edit above the fold must be
  byte-neutral or negative. `validate-reattach-budget.sh` is the judge.
- `scripts/install.sh` and `scripts/uninstall.sh` have **hardcoded** copy loops. A new
  script or fixture that is not added to both never reaches a consumer. This trap has
  bitten three times.
- Rule 26(a)/(b): no speculative mechanism; if you add mechanism, state why the simpler
  path fails. The adversary now files unrequested mechanism as **MAJOR**.
- **A check that cannot fail is the bug.** Every new check must be shown going **red**
  against the real defect before it goes green. A fixture that exercises nothing new is
  a vacuous green — this repo shipped one in v0.48.0 and caught it in v0.49.0.
- Report honestly. If a fix does not land, or a check cannot fire, say so in the
  changelog under a heading that says it.
