# Sprint 301 close-out — the prompt to paste into the graph session

## Start here

**Read this whole file before running anything.** It is a close-out procedure for the
**consumer** repo `/Users/n8/git/graph`, written in the ai-dlc distribution
(`/Users/n8/git/ai-dlc`) and delivered as plan item 9 of
[`retire-graph-consumer-layer.md`](retire-graph-consumer-layer.md). **The direction of the
read/write boundary is INVERTED from the parent plan's, and that is the thing to get right
here.** The parent plan reads graph and writes ai-dlc; this procedure writes **graph** and only
reads ai-dlc: read `/Users/n8/git/ai-dlc`, and do not write to it. The operator, not a session,
is what runs the procedure at all. The operator pastes
§*The prompt* into a graph session; everything above it is the derivation, kept here because a
procedure whose reasons live elsewhere gets followed literally and wrongly.

**Sprint 301 is being ABANDONED, not completed.** It stalled at `stories-test-strategy.md` §4
after eight adversarial passes and never reached implementation. It is re-run from scratch as
s302, the way s300 was. **The re-run is itself the control test**: it measures whether the
releases in the parent plan change the outcome of a sprint that has already failed once
without them.

**There is no `abandoned` status value and no documented abandonment procedure in the
distribution.** The machinery knows `in_progress` and `done`. The abandonment lives entirely in
the `closure_evidence` prose. s300's close-out was improvised against `retro.md:501` +
`route.md:394`; this file exists so s301's is not improvised twice.

**Every number below was re-measured against the consumer on 2026-08-07**, read-only. Where a
figure differs from s300's, that is called out — s300's procedure is the template and its
numbers are NOT transferable.

## Next actions

1. **Operator: read §*The prompt* and paste it into the graph session.** Nothing in this file
   is run from the distribution.
2. **After the close-out lands, run the two-hop pull** (§*pull graph in TWO hops* in the parent
   plan). Hop 2 carries six rulebook files and wants exactly the sprint boundary this close-out
   creates.
3. **Do not start s302 until both are done.** That ordering is the point of the control test.
4. BLOCKED — **do not disposition `LR-S299-0..11`** during this close-out. See difference 3.

## What s300's close-out actually did

**It was TWO commits, not one**: `7ecd99dd1` (the archive) and `ff490ebd0` (the envelope),
about 95 minutes apart with three unrelated commits between them.

`7ecd99dd1` — 4 added, 8 modified, **90 renamed R100, ZERO deleted**. Artifacts were `git mv`'d
into six per-series `archive/s300-*` directories; the gate log was emptied; gate-metrics went
723 → 661; the S300 LOCKED block was excised from `product-brief.md` reversibly. **Verification
was recorded per file as `source-absent AND dest-readable AND sha256-identical` — not a count.**
Two deliberate NON-actions: no Check 33 `NOT-IN-SCOPE` disposition (it would assert a scope
decision nobody made) and no `RESTART_CYCLE` record (all five series terminated
`EXIT_CONDITION_MET`).

`ff490ebd0` — ran `sprint-status.sh close` **first**, then `roll`, because `roll` exits 3 while
the prior sprint is not `done`. The snapshot was regenerated by script, not hand-authored. **The
new branch was cut from HEAD, not `main`** — `main` does not carry `7ecd99dd1`, so branching
from it would have resurrected all 90 archived artifacts.

## Seven differences an s301 close-out must handle

Six were recorded in the parent plan and re-verified here; the seventh is new.

1. **The S301 LOCKED block is an ALIAS of S299, not new content.** `product-brief.md:33` states
   *"The sole LOCKED block surviving this consolidation pass was `S299`."* S300's block was
   original and was excised wholesale. **Excising S301's must not damage S299's still-live
   block.**
2. **`_bmad-output/pipeline-paused.flag` is PRESENT** and had no S300 counterpart.
   `_bmad-output/.beat-inflight` is **absent** — control: the pause flag in the same listing is
   present, so the reader can tell a real absence from a failed check.
3. **s302 will be the THIRD sprint carrying the same unbuilt ask.** S300's reasoning for not
   dispositioning `LR-S299-0..11` has to be **restated in s301's own closure evidence**, not
   inherited. A reader who finds the reasoning only in S300's commit body will not look.
4. **Volumes differ from S300's, in both directions.** Measured:

   | | s300 | s301 |
   |---|---|---|
   | gate-metrics rows for the sprint | 62 | **75** |
   | gate-log lines | 571 | **293** |
   | planning artifacts | 90 | **99** |
   | per-series archive dirs needed | 6 | **9** |

5. **The branch-cut rule repeats.** `main` still does not contain `7ecd99dd1` (re-verified).
   Cut the new branch from **HEAD**.
6. **`core.hooksPath` is unset** (re-verified), so consumer git hooks will not fire during the
   close-out. Nothing will catch a mistake mechanically.
7. **NEW — the gate log's heading grammar is not the one s300's procedure assumed.** It carries
   `## Gate: planning — Sprint 301 (…)`, not `## Gate Log: Sprint N`. A sweep keyed on the
   latter empties nothing and reports success. Control: the file is 293 lines and its headings
   were enumerated, so this is a grammar difference, not an absent file. *(Side effect worth
   knowing: `validate-mandatory-rules.sh` Check 5 keys on `## Gate Log: Sprint N`, so it cannot
   isolate this sprint's section either — that is the parent plan's item 10 territory, not this
   close-out's.)*

## Three inventory traps, each of which reads as a clean result

1. **A prefix-only glob undercounts, and by more than it did for s300.** `ls s301-*` gives
   **69**; `find -name '*s301*'` gives **99**. s300's gap was 81 vs 86. Control that the
   substring form is not simply over-matching: the same two forms over s300 today give **0** and
   **91** — zero for the prefix because those files are already inside `archive/s300-*`, which
   is exactly the state s301's sweep is trying to reach.
2. **`gate-metrics.jsonl` stores `sprint` as an INTEGER.** `grep '"sprint": "301"'` returns
   **0** and looks clean; `grep '"sprint": *301'` returns **75**, out of 736 rows total.
3. **NEW — grepping the corpus for `RESTART_CYCLE` returns 1, and it is a NEGATION.** The single
   hit is `s301-carry-over-evaluation-resolution-p4.md`, which says *"RESTART_CYCLE was not
   warranted — only this one passage stalled."* Control: `EXIT_CONDITION_MET` appears 22 times
   in the same corpus. **So s301, like s300, writes no `RESTART_CYCLE` record — but not for
   s300's reason**, and a close-out that greps for the token will conclude the opposite.

---

## The prompt

*Paste everything below this line into the graph session.*

---

Close out sprint 301. It is being **abandoned, not completed** — it stalled at
`stories-test-strategy.md` §4 after eight adversarial passes and never reached implementation.
It will be re-run from scratch as s302. Mirror the s300 close-out (`7ecd99dd1` + `ff490ebd0`),
which is the only precedent; there is no documented abandonment procedure.

**Do it as TWO commits, in this order.**

**Commit 1 — the archive.** `git mv` only. **Never delete.**

- Enumerate the artifact set with `find _bmad-output -name '*s301*' -type f`, **not** a
  `s301-*` prefix glob. The prefix form returns 69 files; the substring form returns 99. Report
  both numbers in the commit body so the gap is on the record.
- Create one `archive/s301-<series>` directory per series and `git mv` its artifacts in. The
  series are: `s301-stories` (17), `s301-coe` (8), `s301-architecture` (7), `s301-prd` (6),
  `s301-product-brief` (5), `s301-epics`, `s301-carry-over-evaluation`, and the two
  `s301-test-strategy-*` files. **Nine directories** — s300 needed six.
- **Verify per file, not by count**: for each moved file assert `source-absent AND
  dest-readable AND sha256-identical`, and record that phrasing. A count of 99 moved files
  proves nothing about which 99.
- Empty the gate log. **Its headings are `## Gate: planning — Sprint 301 (…)`, NOT
  `## Gate Log: Sprint N`** — a sweep keyed on the latter matches nothing and exits clean.
- Reduce `gate-metrics.jsonl`. The sprint field is an **integer**: `grep '"sprint": *301'`
  finds **75** rows of 736. `grep '"sprint": "301"'` finds **0** and looks clean. Report the
  before/after row totals.
- Excise the S301 LOCKED block from `product-brief.md`, reversibly. **S301's block is an ALIAS
  of S299's, and S299's is the sole surviving in-force block (`product-brief.md:33`). Do not
  touch S299's.** Diff the file afterwards and confirm the S299 body is byte-unchanged.
- Clear `_bmad-output/pipeline-paused.flag`. (`.beat-inflight` does not exist — do not create
  or look for it.)

**Two deliberate NON-actions, both of which must be stated in the commit body as decisions:**

- **No Check 33 `NOT-IN-SCOPE` disposition.** Writing one asserts a scope decision that never
  happened.
- **No `RESTART_CYCLE` record.** Twenty-two series terminated `EXIT_CONDITION_MET`. Grepping
  the corpus for `RESTART_CYCLE` returns one hit, in
  `s301-carry-over-evaluation-resolution-p4.md`, and it is prose saying RESTART_CYCLE **was not
  warranted**. That is a negation, not a record.

**Commit 2 — the envelope.**

- Run `sprint-status.sh close` **first**, then `roll`. `roll` exits 3 while the prior sprint is
  not `done`.
- Let the script regenerate the snapshot. Do not hand-author it.
- **Cut the new branch from HEAD, not `main`.** `main` does not contain commit 1, so branching
  from `main` resurrects every archived artifact.
- The `closure_evidence` prose is where the abandonment lives — there is no `abandoned` status
  value. State in it: that s301 is abandoned rather than completed, where it stalled, that it
  re-runs as s302, and **restate (do not cite) S300's reasoning for leaving `LR-S299-0..11`
  undispositioned** — s302 is the third sprint carrying that same unbuilt ask, so a reader will
  not go looking for the reasoning in a commit body two sprints back.

**One environment note:** `core.hooksPath` is unset in this repo, so the git hooks will not fire
during either commit. Nothing here is checked mechanically — the per-file verification above is
the only thing standing in for it.
