# v0.13.0 — Consumer-absorption backport (design record)

Status: **PROPOSED**. Target version: 0.13.0. Provenance: reconciliation diff of
the **graph** consumer @ Sprint 281 against distribution **v0.11.0** (`e7ccffa`,
the last absorption baseline, pre-v0.12.0 relocation). Method: six parallel
classification agents, per-file diff, each block bucketed
ALREADY-UPSTREAM / BACKPORT-WORTHY / GRAPH-LOCAL. Continues the
consumer-absorption arc (v0.4.1, v0.4.2, v0.11.0).

## 1. Context — direction of the sync

Graph is a heavily-customized domain fork, not a stale consumer. Its rulebook is
2–3.7× distribution (gate-validation 1598 vs 841 lines; deploy-validate 827 vs
224) because the bulk is on-chain / financial / ECS / subgraph / SSM machinery
graph needs. Two consequences:

1. **A blanket re-install of graph would reset that domain machinery to
   distribution — a regression, not an upgrade.** The valuable direction is
   graph → distribution (this backport), not distribution → graph.
2. **v0.11.0 did its job.** The tech-agnostic core (AC-authoring, discriminating
   fixtures, dispatch discipline, field-verification, severity classes,
   falsification ladder, prior-decision search) is already upstream. Most graph
   divergence is raw/reworded forms of already-absorbed content — in several
   places distribution is *tighter* than graph (Rule 2/4/19, the 5-step handoff,
   the Rule 26(c) contract, `stories-test-strategy` discipline).

This record captures the residual: generalizable innovation graph accumulated
that the S251–S281 window did not reach — mostly **pre-S251 items**
(S172/S176/S188/S241/S247) plus a few newest (S271–S281). Same philosophy as
v0.11.0: absorb only the tech-agnostic, multi-sprint-validated core; strip graph
specifics; Rule 18 voice; Rule 26(c) contracts inline.

## 2. Tier-1 backports (high-confidence, tech-agnostic) — drafts

Each item: generalized draft (ready to insert), the distribution target after
v0.12.0 relocation, what graph-specific content was stripped, and the Rule 26(c)
contract where the item is machinery.

### 2.1 Foreground-dispatch + dependency-DAG / wave-plan
- **Target:** `steps/implementation.md` — extends the Worktree-explicit dev
  dispatch section (additive; feeds its step 6 between-wave serialization).
- **Draft:**

> **Foreground-dispatch mandate.** A gated story-dev cycle is synchronous: the
> lead's immediate next action reads the dev's result and routes it into gate-1.
> The lead MUST dispatch gated story-dev and fix-forward dev agents in the
> FOREGROUND — a blocking Agent call whose result the lead consumes.
> `run_in_background: true` is PERMITTED ONLY for detached work with no near-term
> gate the lead must consume (monitoring loops, polling a long-running job,
> observation windows during which the lead has other non-dependent work).
> Backgrounding a gated dev cycle detaches the producer from a consumer blocked
> on it and stalls the orchestration turn. Violation is a lead-conduct retro
> finding.
>
> **Foreground-dispatch ≠ serial execution.** "Foreground" governs HOW a dev is
> dispatched (a blocking Agent call the lead consumes), NOT how MANY run at once.
> Independent stories MUST be dispatched in parallel — in ONE message, each in its
> own worktree, joined on all results. Parallelism comes from per-story worktrees
> plus a join, NEVER from `run_in_background`. The lead SHALL serialize two
> stories ONLY on a real dependency: a shared source file both stories write, or a
> by-content gate dependency (story B's gate-1 reads story A's merged output).
> Before the first dispatch the lead MUST emit, as a written planning output, a
> **story dependency-DAG + wave plan**: for each story, the files it owns and the
> stories it genuinely depends on, with wave grouping derived from it (independent
> stories → same wave / parallel; shared-file or by-content chains → serialized
> land-order). A default-to-serial dispatch the operator must challenge to
> parallelize, or a missing / after-the-fact wave-DAG, is a lead-conduct retro
> finding.
>
> **Minimum mechanism (Rule 26(c)) — the wave-DAG planning output.** Failure
> caught: silent over-serialization — independent stories queued behind a chain
> they share no files with, wasting wall-clock and parallel capacity; and its
> inverse, parallel dispatch of two stories that write the same file, causing
> merge thrash. False-positive cost: one written dependency-DAG + wave grouping
> per sprint before dispatch. Removal condition: retire once dispatch parallelism
> is derived mechanically from a per-story file-ownership manifest rather than
> lead judgment.

- **Stripped:** the S272 worked example (specific files/story-IDs, reindex-polling
  / canary-observation detached-work examples), PI tags, origin narrative.
- **Dependencies:** extends the Worktree-explicit dev dispatch protocol; each spawn
  still passes `model` per Rule 19.

### 2.2 Worktree gate-verification freeze
- **Target:** `steps/implementation.md` — within the Worktree-explicit dispatch
  protocol; complements the DAR-fold preflight (disjoint windows, no overlap).
- **Draft:**

> **Worktree gate-verification freeze.** Once the lead dispatches a gate reviewer
> (gate-1 code review or gate-2 QA) against a dev worktree, that worktree is
> FROZEN: the dev MUST NOT push further commits to it until the verdict lands. At
> reviewer dispatch the lead MUST record the exact `git -C <worktree> rev-parse
> HEAD` SHA, and before recording any gate verdict MUST re-confirm `HEAD` still
> equals that SHA. A HEAD advance between dispatch and verdict VOIDS the verdict —
> the lead re-pins the SHA and re-dispatches / re-verifies. The freeze is the fix
> (it prevents the drift); the HEAD-confirm is the tripwire proving the freeze
> held. Violation: gate verdict void + retro finding.
>
> **Minimum mechanism (Rule 26(c)).** Failure caught: a reviewer's SHA-labeled
> verdict desyncing from the worktree file-state — a mid-review commit (even a
> benign dead-code removal) breaks the label→state binding, so the recorded
> verdict certifies a tree that no longer exists. False-positive cost: one
> `rev-parse HEAD` at dispatch plus one equality re-check at verdict — two cheap
> reads, no dev-side work when the freeze is honored. Removal condition: retire
> once the gate pipeline pins reviews to an immutable content ref (a tag or a PR
> merge-commit) instead of a live worktree HEAD.

- **Stripped:** PI tag + origin narrative (source had no graph-specific names).
- **Dependencies:** operates on the per-story worktree from the dispatch protocol;
  enforcement rides the same Check-15 / retro path.

### 2.3 Local-tree freshness precondition
- **Target:** `steps/sprint-review.md` — new Execution-Sequence Step 0, before any
  sprint code is read. Read-time complement to the merge-time freshness gate.
- **Draft:**

> ### 0. Local-tree freshness precondition
>
> Before any sprint code is read for review, the reviewer MUST assert the local
> checkout is not behind the integration branch. Run:
> ```bash
> git fetch origin main
> git rev-list --count HEAD..origin/main   # MUST be 0
> ```
> A non-zero count means the local tree is STALE — it is missing commits already
> merged to `origin/main` (e.g. a squash-merged sprint PR), so the review (and any
> deploy built from this tree) would read code that does NOT match the
> release-bound branch. STOP: fast-forward or rebase onto `origin/main`, then
> restart the review on the fresh tree.
>
> **Minimum mechanism (Rule 26(c)).** Failure caught: a review/deploy that reads a
> local tree missing commits already merged upstream (stale-tree drift-clobber).
> False-positive cost: effectively none — count 0 is the steady state; cost is one
> `git fetch` per review. Removal condition: retire only if read-time freshness
> becomes structurally guaranteed (reviews always run on a freshly-checked-out
> `origin/main`-current tree).

- **Stripped:** PI origin tags, the "GitHub `BEHIND` asserted only at merge time"
  narrative, internal drift-clobber label.
- **Dependencies:** shares the integration-branch assumption with deploy-validate
  (§2.4 builds from the tree this asserts fresh).

### 2.4 Deploy-freshness digest gate
- **Target:** `steps/deploy-validate.md` — hard gate AFTER deploy, BEFORE smoke.
  Pairs with §2.7-style gate-check that verifies it ran.
- **Draft:**

> ### Deploy-freshness digest gate (Hard Gate — Non-Skippable)
>
> **Placement: AFTER deploy, BEFORE smoke tests.** For EVERY deployable unit with
> changed paths in the sprint diff, the lead MUST prove the just-built artifact is
> the one actually running before smoke tests run:
> 1. Read the CONTENT DIGEST of the artifact currently running in the target
>    environment.
> 2. It MUST equal the content digest of the artifact just built and pushed for
>    this sprint; also confirm the rollout/start timestamp is newer than the merge.
> 3. If the running digest does NOT match the built digest for any changed unit, the
>    deploy did NOT happen: the gate FAILS, smoke tests MUST NOT run, and the lead
>    returns to the deploy step.
>
> **Freshness MUST be asserted on the running-artifact CONTENT DIGEST, never on a
> re-pointable pointer** (image tag, deployment/task revision id, release ARN or
> handle). A force-redeploy re-pulls the SAME revision, so such a pointer is
> EXPECTED to stay UNCHANGED after a successful redeploy — asserting freshness on an
> unchanged pointer is a FALSE-FAIL that masks a real deploy.
>
> **Minimum mechanism (Rule 26(c)).** Failure caught: merge succeeded but the new
> build never became the running artifact (stale deploy), so smoke would green-light
> the previous sprint's code. False-positive cost: a legitimate no-op redeploy where
> the built digest genuinely equals the prior running digest — resolve by confirming
> the rollout timestamp is post-merge, not by digest alone. Removal condition: retire
> only if deploy tooling is made to fail closed on a non-fresh rollout.

- **Stripped:** the AWS/ECS/ECR command block, task-def-ARN specifics (→ generic
  "re-pointable pointer"), snapshot Deploy-Baseline path, S-tags.
- **Dependencies:** hard-precedes the smoke-evidence gate; depends on a per-unit
  changed-path applicability predicate for scope.

### 2.5 Named-field-vs-implementation divergence gate
- **Target:** `steps/architecture.md` (ADR/design section, near Rule 26 selection).
  Couples to `code-reviewer.md` Rule 26(c) audit (reviewer verifies the gate ran).
- **Draft:**

> **Named-field-vs-implementation divergence gate.** For every named field, entity,
> or invariant an ADR introduces, the architect MUST execute this four-step gate
> before the ADR merges:
>
> 1. Write the NAME as a plain-English sentence stating what it claims — what a
>    downstream consumer would assume it means from the name alone.
> 2. Write the IMPLEMENTATION contract as a sentence stating what the code actually
>    computes or guarantees, including its start conditions, boundaries, and
>    accumulation/derivation rules.
> 3. Diff (1) against (2). Any token in the name not faithfully represented by the
>    implementation is a divergence. Resolve by exactly ONE of: (a) rename the field
>    to match the implementation; (b) extend the implementation to match the name;
>    or (c) document the gap in the ADR with an explicit `consumer-MUST-read`
>    warning AND expose a sibling field carrying the boundary/anchor the name omits.
> 4. Include a mandatory consumer-side usage example in the ADR body, written as the
>    downstream consumer would write it. If the example needs a corrective caveat
>    ("note: this is actually X, not what the name implies"), the field is
>    mis-named — return to step 3.
>
> The gate is non-skippable. The architecture step FAILS if any newly named
> field/entity/invariant lacks BOTH (a) evidence of the four-step check in the
> commit log or ADR body AND (b) the consumer-side usage example.
>
> **Minimum mechanism (Rule 26(c)).** Failure caught: a named field whose name
> over-claims what the implementation guarantees, so a downstream consumer trusts
> the name, assumes the fuller meaning, and computes against a boundary the code
> never established. False-positive cost: one or two authoring sentences plus one
> example for a genuinely faithful name. Removal condition: retire once every ADR
> field name is mechanically validated against its implementation contract (a
> naming linter over the schema), or the pipeline no longer admits named-field
> ADRs.

- **Stripped:** the specific financial field/anchor worked example, subgraph-deploy
  framing — replaced with neutral "named field/entity/invariant" language.
- **Dependencies:** resolution path (c) documented-gap → keep vocabulary consistent
  with Rule 12 DECIDED_AUTONOMOUSLY when contentious; dedupe against any existing
  ADR "consumer-facing contract" clause.

### 2.6 HARD_BLOCK Evidence / Assertion epistemic-hygiene pair
- **Target:** `escalations.md` — two new fields in the entry-format block, between
  `**Context:**` and `**Decision/Question:**`, + enforcing prose; governed by
  SKILL.md Rule 12 (only HARD_BLOCK requires both populated). v0.12.0 relocated the
  entry format to `escalations.md`, so this lands there and propagates to all four
  role filers without per-role edits.
- **Draft — add to the entry-format code block:**

> ```
> **Evidence observed:** [HARD_BLOCK REQUIRED: only concrete, directly-verifiable facts actually observed — log lines anchored to file:line, command output, measured values, tool results, file contents. Nothing inferred.]
> **Assertion (beyond evidence):** [HARD_BLOCK REQUIRED: every claim, inference, or root-cause hypothesis that goes beyond the evidence above. If there is none, write "none — assertion equals evidence." A handoff successor MUST treat this field as unverified until independently re-confirmed.]
> ```

- **Draft — enforcing prose (below the block):**

> The `Evidence observed:` / `Assertion (beyond evidence):` pair is MANDATORY on
> every HARD_BLOCK. A HARD_BLOCK that conflates an observed fact with an inferred
> root cause propagates the inference as settled truth across a handoff or a
> checkpoint review — the successor or operator then acts on an unverified
> assertion as if it were evidence. Separating the two forces the filer to mark the
> verified floor explicitly and quarantine every claim above it. A HARD_BLOCK
> missing this pair is malformed and MUST be corrected before it is presented at the
> production validation checkpoint.
>
> **Minimum mechanism (Rule 26(c)).** Failure caught: a HARD_BLOCK whose root-cause
> hypothesis is stated as established fact, so a post-handoff successor or the
> checkpoint operator acts on an unverified inference as proven. False-positive
> cost: one line ("none — assertion equals evidence") when a blocker genuinely has
> no inference beyond its evidence. Removal condition: retire if handoffs and the
> production validation checkpoint are both eliminated, or a separate verification
> step independently re-confirms every root-cause claim before it can be acted on.

- **Stripped:** the on-chain-reads evidence example, the operator-ratified/S247
  provenance tag, graph "PVC" abbreviation (→ "production validation checkpoint").
- **Dependencies:** Rule 12 owns the three tiers (only HARD_BLOCK requires both
  fields; DECIDED/DEFERRAL may leave Assertion "none"); Rule 11 files through the
  same format; align the "successor MUST treat as unverified" clause with
  `handoff.md`'s successor contract (align, don't duplicate).

### 2.7 Validation-intensity compliance gate
- **Target:** `steps/gate-validation.md` — a NEW numbered check. Distribution tops
  out at Check 19 → assign **Check 20** (re-confirm at insertion; do NOT reuse
  graph's "21"). This is the gate-side enforcement of Rule 8's existing mandate
  ("the gate log MUST record the declared intensity and confirm the minimum was
  met"), which currently has NO gate check. Depends on `validation_intensity` in
  the snapshot Sprint Context — already present since v0.12.0 (Check 14).
- **Draft:**

> ### 20. Validation-intensity compliance (all planning gates).
>
> **Scope.** Fires at every planning-phase gate. Skips for implementation,
> deploy-validate, and retro gates.
>
> **Check.** Read `validation_intensity` from the pipeline snapshot's Sprint
> Context. Confirm the validation cycle run at this gate met at least the declared
> intensity's minimum:
> - `full`: Party Mode + Advanced Elicitation + Adversarial Review (2+ passes) —
>   all three MUST have been invoked.
> - `standard`: Party Mode + Adversarial Review (1+ pass) — both MUST have been
>   invoked.
> - `lightweight`: Adversarial Review (1 pass) MUST have been invoked.
>
> An architecture gate that reaches a NO-CHANGES-NEEDED assessment MAY skip the
> validation cycle (fast-track). The gate log entry MUST record
> `validation_intensity: <level>` and `minimum_met: true|false`. The check FAILS if
> the declared minimum was not met. Declared intensity MUST NOT reduce the
> always-required floors — carry-over-eval Party Mode, retro Party Mode, and
> deploy-validate smoke remain mandatory at every intensity.
>
> **Minimum mechanism (Rule 26(c)).** Failure caught: a planning gate that under-ran
> its declared intensity (a `full` sprint that skipped Advanced Elicitation or ran a
> single adversarial pass), silently downgrading operator-selected rigor.
> False-positive cost: a sanctioned fast-track (architecture NO-CHANGES-NEEDED)
> counted as a miss — resolve by recording skip provenance in the gate log, not by
> failing. Removal condition: retire once validation-cycle invocation is made
> structurally unskippable per intensity.

- **Stripped:** the enumerated graph gate-name list, the `carry-over-single` tier +
  `/bmad-*` sanctioned-skip sub-skill names, the carry-over-provenance menu-skip
  clause, internal fast-track tag.
- **Number mapping:** graph Check 21 → distribution **Check 20** (see §7).
- **Dependencies:** intensity-graded teeth on top of Check 1 (which only asserts a
  cycle ran). Requires the gate-log schema to accept `validation_intensity` +
  `minimum_met` — confirm at wiring, else the read fails vacuously. Scope guard MUST
  exclude implementation/deploy-validate/retro gates and MUST NOT reduce the
  always-on floors it names.

## 3. Tier-2 candidates (solid; de-graph or verify-not-already-absorbed first)

Documented now, deferred to a follow-up pass — each needs generalization work or a
confirm-absent check against v0.12.0 before drafting:

- **Check 33 — cross-story test-strategy §3 deliverable presence** (gate-validation,
  S281): every test named in the test strategy exists on disk and is cited from a
  Dev Agent Record; fail-closed.
- **Check 29 — merge-approval does not survive a session handoff** (gate-validation,
  S253-6): directly relevant to the v0.11.x auto-session-chaining workstream;
  overlaps the existing "pending approvals do not transfer" mandate — reconcile
  rather than duplicate.
- **Perf-class AC input-shape + representative-load regime** (qa.md, QA-S172-2):
  any AC bounding a perf metric names the (K,N,depth) regime where the bound holds
  and tests at an upper-bound-of-observed-load fixture.
- **Deferred-AC discharge predicate** (qa.md, PI-S271-7): each deferred/deploy-pending
  AC names the exact downstream predicate that discharges it; a bare "deferred" =
  REJECT; deploy-validate verifies each predicate green before done.
- **Validity-guard observability severity** (code-reviewer.md, CR-S176-5,
  de-financialized): a PR adding suppress/clamp/null-emit guards on a consumer-facing
  data surface MUST ship observability (log/metric/smoke probe); silent
  None/sentinel emission = Critical.
- **AC verification-category-change disclosure** (SKILL.md): on HARD_BLOCK
  resolution, `AC N category change old→new. Operator ack Y/N`.
- **Live-security-state autonomous-mutation carve-out** (SKILL.md, PI-S271-2):
  the agent MUST NOT autonomously mutate live access-control/security state; stage
  it and let the operator fire; record per-action in-session authorization. Tightens
  autonomy.
- **Escalation-log terminal-entry archival** (retro.md + SKILL.md, Rule 25(a)):
  move RESOLVED / OVERRIDDEN entries out of `pending.md` into an archive so the
  gate-read stays bounded to open escalations. Fits the v0.9.0/Rule 25 no-loss
  archival family.
- **LOCKED_REQUIREMENT deferral needs recorded operator disposition** (retro.md):
  same-sprint delivery does not cleanse a deferral.

## 4. Tier-3 (weak / verify / heavy de-graph — likely leave)

**Interrogated against v0.14.0 (post Tier-1 + Tier-2 absorption). The
"likely leave" assumption HELD for all five — none absorbed. Verdicts +
evidence recorded so the next reconciliation does not re-triage.**

- Mid-sprint scope re-check trigger (impl PI-S241-2) — nuanced, lower
  confidence. **LEAVE.** `implementation.md` already carries per-commit scope
  verification (dev runs `git diff --staged`, verifies every changed file is
  in the story's stated scope, out-of-scope changes stashed / committed
  separately). Graph's mid-sprint trigger overlaps this; marginal add over
  the existing per-commit guard.
- git-safety guards (retro PI-S271-6) — verify not already absorbed
  elsewhere. **LEAVE (by no-confirmable-gap, not confirmed-redundancy).**
  Distribution git-safety surface is already rich: worktree stash-collision
  guard (impl dispatch step 6), pre-gate commit-presence, canonical-story
  pre-flight, worktree HEAD-freeze (§2.2), local-tree freshness (§2.3). No
  specific gap is confirmable without the graph PI-S271-6 retro text — the
  one item whose "leave" rests on absence of a citable gap rather than proven
  overlap. Re-open only if that retro names a guard not in the list above.
- Decision-branch execution-coverage (sprint-review) — remedy is live-ops
  flavored. **LEAVE.** The tech-agnostic kernel (every decision branch needs
  execution/test coverage) is already served by `qa.md`'s discriminator-AC
  mutation-RED + per-locked-requirement adversarial null-impl discipline. The
  graph remedy is production-branch-observation (live-ops) flavored and does
  not generalize.
- Authoritative `has_ready_sprint` computation (route.md) — tangled with
  graph's append-only status-file shape; backport only the principle,
  conservatively. **LEAVE.** `route.md` already defines `has_ready_sprint`
  (boolean: stories with status ready-for-dev / in-progress, read from
  `sprint-status.yaml`) and consumes it in the routing decision tree. The
  authoritative-computation machinery is specific to graph's append-only
  status-file shape, which distribution does not use; the principle
  (compute from the status source, do not infer) is already embodied.
- feature-active ≠ feature-in-code pair (deploy-validate, S188/S245) — verify
  against v0.11.0's pre-deploy step before lifting; may already be upstream.
  **ALREADY UPSTREAM (confirmed).** `deploy-validate.md` "Config-gated feature
  activation check" states verbatim *"'The feature exists in the code' is NOT
  evidence it is active in production"* and pairs with the "Post-activation
  live-log check". The pair is fully present — nothing to lift.

## 5. Explicitly NOT backported (graph-local — install would destroy these)

Domain machinery graph depends on; distribution intentionally does not carry it:
- gate-validation Checks 20,23,24,26,27,28 (HPE-ADR probe evidence, financial
  ground-truth live-verify, deployed-ranges consistency, config-integrity vclass,
  execution-health 4-floor) and retired Check 32.
- deploy-validate: execution-health floors, deployed-ranges gate, smoke env-var
  contract, named smoke drivers, apr/reindex/live-UI checks.
- implementation: deployed-response-fidelity (mTLS/Flask), done-pending-liveness,
  story-status-consistency script, AWS tooling.
- Sprint-type path lists, subgraph prune retention, SSM port-forward, config
  dual-schema mirror; role-file OPUS_REVIEW_PATTERNS financial symbols, CWD
  mandate, SafeguardContext suites, concrete model strings.

## 6. Held / structural

- **`guarded-merge.sh`** (graph hook) — the deliberately-held family (0 true
  catches, per prior retros). Not `ai-dlc-*`-named, so install.sh never touches
  it; it survives on graph. Stays held; not a fresh candidate (gate-validation
  Check 25, post-merge fail-closed audit, is the same family).
- **`ai-dlc-driver-signal.sh`** (distribution v0.11.x auto-chaining hook) — graph
  LACKS it. The one genuine distribution-ahead item graph could cherry-pick DOWN
  (file-scoped copy, never a blanket install).
- **`tea.md`** — graph-only team role (test-engineer/architect). Distribution
  lacks it. Separate decision: generalize a test-engineer role upstream, or leave
  graph-local. Not part of this backport.

## 7. Renumbering hazard (load-bearing)

Graph's gate-validation Checks 20–33 are net-new numbered checks. Distribution
currently stops at Check 19. Any distribution addition of a numbered Check ≥20
(e.g. Tier-1 §2.7 → Check 20) will collide with graph's numbering on a future
diff. On backport, assign the distribution number from distribution's own
sequence (NOT graph's), and record the graph→distribution number mapping in this
spec so the next reconciliation does not re-flag an already-absorbed check as new.

Mapping (this backport): graph Check 21 (validation-intensity) → distribution
Check 20.

## 8. Rollout

- **v0.13.0 Phase 1:** apply the Tier-1 §2 drafts to the distribution files after
  final review. Each is behavior-adding, so bump MINOR and add a CHANGELOG entry.
  Run the v0.12.0 retro guardrails (pointer-resolve + first-5K) after — Tier-1
  §2.6/§2.7 touch resident SKILL.md / gate-validation.
- **v0.13.x Phase 2:** the Tier-2 set, after per-item de-graphing / confirm-absent.
- **Not automated for graph.** This backport lands in distribution. Graph does not
  re-install to receive it (that would reset its domain fork); the value is that
  distribution now carries graph's generalizable innovations for the *next*
  greenfield consumer, and future graph reconciliations shrink.

## 9. Verification

- Every Tier-1 draft is graph-name-free (no `server/`/`rebalancer/`/AWS/subgraph/
  financial terms), imperative voice, Rule 26(c) contract where machinery.
- New gate-validation check numbered from distribution's sequence, mapping recorded (§7).
- After apply: SKILL.md first-5K + pointer-resolve guardrails green (v0.12.0
  retro Step 4).
- Full reconciliation ledger (all buckets, all files): the S281 reconciliation
  classification behind this record.
